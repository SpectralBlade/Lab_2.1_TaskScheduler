# Лабораторная работа №1: "Источники задач и контракты"

## Введение
Была реализована подсистема приёма задач (база) для платформы обработки задач с помощью утиной типизации ("Duck Typing"). Модель задачи Task реализована с помощью `__slots__` (решил попробовать такой подход после того, как узнал о нем на семинаре)

## Структура проекта

 <pre>
    .
    └── Labs1/                                 # Кодовая база лабораторной работы
        ├── src/                               # Исходный код проекта
        │   ├── classes/                       # Основные бизнес-сущности и логика
        │   │   ├── contract.py                # Определение интерфейса (Protocol) TaskSource
        │   │   ├── descriptors.py             # Дескрипторы для валидации
        │   │   ├── exceptions.py              # Классы исключений
        │   │   ├── task.py                    # Модель задачи Task
        │   │   └── validator.py               # Валидатор источников
        │   ├── examples/                      # Примеры источников задач
        │   │   ├── example.json               # Данные для файлового источника
        │   │   └── examples.py                # Список источников-примеров
        │   ├── logging_tools/                 # Инструментарий для логирования
        │   │   ├── config.py                  # Словарь LOGGING_CONFIG
        │   │   └── log_manager.py             # Менеджер инициализации логов и декоратор мониторинга
        │   ├── cli.py                         # Интерактивная оболочка (возможность юзеру самому загружать источники)
        │   ├── main.py                        # Точка входа в приложение
        │   ├── sources.py                     # Реализация источников (Generator, File, Api)
        │   └──task_history.log                # Файл с логами по обработанным источникам
        ├── tests/                             # Unit-тесты (pytest)
        │   ├── conftest.py                    # Общие фикстуры и настройка тестов
        │   ├── test_sources.py                # Тестирование логики получения задач
        │   ├── test_task.py                   # Тесты интерактивной оболочки
        │   ├── test_task.py                   # Тесты доменной модели, дескрипторов, вычисляемых свойств и исключений
        │   └── test_validator.py              # Тестирование валидации протоколов
        ├── .gitignore                         # Список игнорируемых файлов для Git
        ├── README.md                          # Описание проекта и руководство по запуску
        └── requirements.txt                   # Список необходимых зависимостей
</pre>

## Схема работы
Пользователь запускает программу через точку входа - main.py. Ему предлагается ввести имя файла со списком задач для парсинга. После ввода, если все успешно (файл существует и в нем есть список задач) происходит парсинг источников, сбор задач с них и параллельная запись в кеш/логи. После обработки источников пользователь может взаимодействовать с интерактивной консолью (CLI) - просмотреть все спаршенные источники, увидеть все задачи или задачи какого-то конкретного источника.

## Пользовательские допущения
Для корректной обработки данных:
- Да, источники задач в данный момент читаются из файла
- Файл с источниками должен находиться по пути src/examples
- Файл должен быть в формате .py
- Источники задач должны храниться в списке с названием SOURCES
- В директории уже есть файл examples.py для теста

Удачного пользования парсером задач!

# UPD: Лабораторная работа №2: "Модель задачи: дескрипторы и @property"
1. Переделана модель задачи (помимо id и payload добавлены новые поля)
2. Реализованы два data дескриптора (для управления приоритетом/статусом задачи и жесткой инкапсуляции+валидации).
3. Реализован один non-data дескриптор (для генерации краткой информации о задаче)
4. Добавлено вычисляемое свойство в модель задачи через @property (is_ready - позволяет понять, готова ли задача к выполнению)
5. Обновлен метод fetch_and_display в валидаторе источника для корректного отображения (с использованием новой сводки - summary)
6. Отредактированы классы (примеры) источников задач для соответствия текущей модели задачи.
7. Обновлены тесты для источников задач и добавлены новые (test_task) для тестирования свойств и дескрипторов.